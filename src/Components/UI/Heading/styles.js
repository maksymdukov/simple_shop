const styles = (theme) => ({
    customHeading: {
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        margin: "40px 0 10px 0",
        fontWeight: 300
    }
});

export default styles;