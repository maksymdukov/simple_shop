const styles = theme => ({
    message: {
        textAlign: "center"
    },
    title: {
        textAlign: "center"
    },
    container: {
        padding: theme.spacing.unit * 5
    }
});

export default styles;
