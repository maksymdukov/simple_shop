const styles = theme => ({
    addToBasket: {
        // backgroundColor: "green",
        // color: "white"
        borderRadius: 0,
        '&:hover': {
            opacity: 0.9
        }
    }
});

export default styles;