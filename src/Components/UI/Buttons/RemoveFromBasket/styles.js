const styles = theme => ({
    removeFromBasket: {
        color: "inherit",
        borderRadius: 0,
        "&:hover": {
            opacity: 0.9
        }
    }
});

export default styles;
