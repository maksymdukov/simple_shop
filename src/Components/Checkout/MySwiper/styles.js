const styles = (theme) => ({
    container: {
        marginTop: 20
    },
    card: {
        background: "inherit",
        width: 250,
        minWidth: 250,
        margin: "20px 0"
    }

});

export default styles;